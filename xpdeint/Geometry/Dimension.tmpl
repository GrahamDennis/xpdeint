@*
Dimension.tmpl

Created by Graham Dennis on 2008-03-19.
Copyright (c) 2008 __MyCompanyName__. All rights reserved.
*@
@extends xpdeint.Geometry._Dimension

@attr $type = 'none'

@def definesWithPrefix($prefix)
  @#
#define ${prefix}_lattice_${name}  $lattice
#define ${prefix}_local_lattice_${name} ${prefix}_lattice_${name}
#define ${prefix}_local_offset_${name} 0
#define ${prefix}_min_${name}  (($type)$minimum)
#define ${prefix}_max_${name}  (($type)$maximum)
  @#
@end def

